/**
 * Team sizes that have ranked ladder stats and matchmaking queue tabs.
 * Keep in sync with `/matches/queues` tabs and admin queue team size options.
 */
export const RANKED_TEAM_SIZES: readonly number[] = [1, 2, 4, 5, 8];
